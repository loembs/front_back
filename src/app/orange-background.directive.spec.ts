import { OrangeBackgroundDirective } from './orange-background.directive';

describe('OrangeBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new OrangeBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
